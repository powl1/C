#define _CRT_SECURE_NO_WARNINGS  // scanf 보안 경고로 인한 컴파일 에러 방지
#include <stdio.h>
/*
int main() { 

	int choice;
	printf("변환할 진수를 선택하세요: 1. 10진수 2. 16진수 3. 8진수\n");
	scanf("%d", &choice);

	if (choice == 1) {
		int num;
		printf("숫자를 입력하세요: ");
		scanf("%d", &num);
		printf("10진수: %d\n", num);
	}
	else if (choice == 2) {
		int num;
		printf("숫자를 입력하세요: ");
		scanf("%d", &num);
		printf("16진수: %x\n", num);
	}
	else if (choice == 3) {
		int num;
		printf("숫자를 입력하세요: ");
		scanf("%d", &num);
		printf("8진수: %o\n", num);
	}
	else {
		printf("유효하지 않은 선택입니다.\n");
	}

}*/